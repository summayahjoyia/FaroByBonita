<?xml version="1.0" encoding="utf-8"?>
<!--
    res/values-night/themes.xml  (dark mode overrides)
    Android automatically uses this file when the device is in dark mode.
    Colors resolve from res/values-night/colors.xml automatically.
-->
<resources>

    <style name="Base.Theme.FaroCaretaker" parent="Theme.Material3.DayNight.NoActionBar">
        <item name="colorPrimary">@color/black</item>
        <item name="colorOnPrimary">@color/white</item>
        <item name="colorSurface">@color/white</item>
        <item name="colorOnSurface">@color/black</item>
        <item name="android:windowBackground">@color/light_grey</item>
        <item name="android:statusBarColor">@color/white</item>
        <item name="android:navigationBarColor">@color/white</item>
    </style>

    <!-- Main app theme -->
    <style name="Theme.FaroCaretaker" parent="Base.Theme.FaroCaretaker" />

    <!-- Splash theme -->
    <style name="Theme.FaroCaretaker.Splash" parent="Theme.FaroCaretaker">
        <item name="android:windowBackground">@color/white</item>
        <item name="android:statusBarColor">@color/white</item>
        <item name="android:navigationBarColor">@color/white</item>
    </style>

</resources>
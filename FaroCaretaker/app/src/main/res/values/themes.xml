<?xml version="1.0" encoding="utf-8"?>
<!--
    res/values/themes.xml  (light mode)
    Night mode overrides go in res/values-night/themes.xml
-->
<resources>

    <!-- Base theme: Material3 DayNight handles automatic dark/light switching -->
    <style name="Base.Theme.FaroCaretaker" parent="Theme.Material3.DayNight.NoActionBar">
        <item name="colorPrimary">@color/black</item>
        <item name="colorOnPrimary">@color/white</item>
        <item name="colorSurface">@color/white</item>
        <item name="colorOnSurface">@color/black</item>
        <item name="android:windowBackground">@color/light_grey</item>
        <item name="android:statusBarColor">@color/white</item>
        <item name="android:navigationBarColor">@color/white</item>
    </style>

    <!-- Main app theme -->
    <style name="Theme.FaroCaretaker" parent="Base.Theme.FaroCaretaker" />

    <!-- Splash theme: plain white, no action bar -->
    <style name="Theme.FaroCaretaker.Splash" parent="Theme.FaroCaretaker">
        <item name="android:windowBackground">@color/white</item>
        <item name="android:statusBarColor">@color/white</item>
        <item name="android:navigationBarColor">@color/white</item>
    </style>

    <!-- Toolbar title style -->
    <style name="ToolbarTitleStyle" parent="TextAppearance.Material3.TitleLarge">
        <item name="android:fontFamily">sans-serif-medium</item>
        <item name="android:textSize">18sp</item>
        <item name="android:textColor">@color/black</item>
        <item name="android:letterSpacing">0.02</item>
    </style>

    <!-- Bottom nav active indicator pill -->
    <style name="BottomNavIndicatorStyle">
        <item name="android:color">@color/grey</item>
        <item name="shapeAppearance">@style/BottomNavIndicatorShape</item>
    </style>

    <style name="BottomNavIndicatorShape" parent="">
        <item name="cornerFamily">rounded</item>
        <item name="cornerSize">50%</item>
    </style>

</resources>
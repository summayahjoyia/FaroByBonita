<?xml version="1.0" encoding="utf-8"?>
<!--
    Styles snippet for CareLink caregiver app.
    Merge these into your existing res/values/themes.xml or styles.xml.

    Make sure your app theme extends a MaterialComponents theme, e.g.:
        Theme.MaterialComponents.DayNight.NoActionBar
    The DayNight variant is what activates the values-night/ folder automatically.
-->
<resources>

    <!-- App theme: DayNight automatically switches between light and dark -->
    <style name="Theme.CareLink" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <item name="colorPrimary">@color/black</item>
        <item name="colorOnPrimary">@color/white</item>
        <item name="colorSurface">@color/white</item>
        <item name="colorOnSurface">@color/black</item>
        <item name="android:windowBackground">@color/light_grey</item>
        <item name="android:statusBarColor">@color/white</item>
        <item name="android:navigationBarColor">@color/white</item>
    </style>

    <!-- Toolbar title: clean, slightly weighted -->
    <style name="ToolbarTitleStyle" parent="TextAppearance.MaterialComponents.Headline6">
        <item name="android:fontFamily">sans-serif-medium</item>
        <item name="android:textSize">18sp</item>
        <item name="android:textColor">@color/black</item>
        <item name="android:letterSpacing">0.02</item>
    </style>

    <!-- Bottom nav active indicator pill: use grey tint, no harsh background -->
    <style name="BottomNavIndicatorStyle">
        <item name="android:color">@color/grey</item>
        <item name="shapeAppearance">@style/BottomNavIndicatorShape</item>
    </style>

    <style name="BottomNavIndicatorShape" parent="">
        <item name="cornerFamily">rounded</item>
        <item name="cornerSize">50%</item>
    </style>

</resources>